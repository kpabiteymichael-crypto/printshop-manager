services:
  - type: web
    name: printshop-api
    runtime: node
    plan: starter
    region: oregon
    buildCommand: npm install
    startCommand: NODE_ENV=production npx tsx server/index.ts
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        fromDatabase:
          name: printshop-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: SEED_SECRET
        generateValue: true
      - key: APP_URL
        value: https://printshop-manager.vercel.app
      - key: ALLOWED_ORIGINS
        value: ""
      - key: LOG_LEVEL
        value: info
      - key: OPENAI_API_KEY
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASS
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: SENTRY_DSN
        sync: false

databases:
  - name: printshop-db
    plan: starter
    region: oregon
    databaseName: printshop
    user: printshop
